name: docker-session-app

services:
  react-app:
    build: 
      context: ./react-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./react-app/src:/app/src
      - ./react-app/public:/app/public
